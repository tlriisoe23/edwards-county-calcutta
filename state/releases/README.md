# Release records

One JSON record per applied release, carrying its environment, commit and authorization evidence. Queue status is not a release claim. The narrative deployment log stays in `docs/VM-DEPLOYMENT.md`.
